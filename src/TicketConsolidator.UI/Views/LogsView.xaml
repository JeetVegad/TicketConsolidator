<UserControl x:Class="TicketConsolidator.UI.Views.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid Margin="20">

        <Grid.Resources>
            <Storyboard x:Key="PulseAnimation" RepeatBehavior="Forever" AutoReverse="True">
                <DoubleAnimation Storyboard.TargetName="LiveIcon" Storyboard.TargetProperty="Opacity" From="0.4" To="1.0" Duration="0:0:1"/>
                <DoubleAnimation Storyboard.TargetName="LiveIcon" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" From="0.8" To="1.1" Duration="0:0:1"/>
                <DoubleAnimation Storyboard.TargetName="LiveIcon" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" From="0.8" To="1.1" Duration="0:0:1"/>
            </Storyboard>
            
            <Style x:Key="LogCardStyle" TargetType="materialDesign:Card">
                <Style.Triggers>
                    <EventTrigger RoutedEvent="Loaded">
                       <BeginStoryboard>
                           <Storyboard>
                               <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.5"/>
                               <ThicknessAnimation Storyboard.TargetProperty="Margin" From="0,20,0,15" To="0,0,0,15" Duration="0:0:0.5">
                                   <ThicknessAnimation.EasingFunction>
                                       <QuadraticEase EasingMode="EaseOut"/>
                                   </ThicknessAnimation.EasingFunction>
                               </ThicknessAnimation>
                           </Storyboard>
                       </BeginStoryboard>
                    </EventTrigger>
                </Style.Triggers>
            </Style>
        </Grid.Resources>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <materialDesign:PackIcon Kind="History" Foreground="{DynamicResource PrimaryHueMidBrush}" Width="32" Height="32" VerticalAlignment="Center" Margin="0,0,15,0"/>
            <TextBlock Text="Logs" Style="{StaticResource MaterialDesignHeadline4TextBlock}" VerticalAlignment="Center"/>
            
            <Border Margin="50,0,0,0" BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="1" CornerRadius="4" Padding="10,0,10,0" VerticalAlignment="Center">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <materialDesign:PackIcon Kind="FilterOutline" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{DynamicResource MaterialDesignBodyLight}"/>
                    <ComboBox ItemsSource="{Binding AvailableFilters}" SelectedItem="{Binding SelectedFilter}" Width="180" materialDesign:HintAssist.Hint="Filter Module" Style="{StaticResource MaterialDesignFloatingHintComboBox}" VerticalAlignment="Center" Margin="0,0,0,10"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding SessionsView}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <materialDesign:Card Margin="0,0,0,15" materialDesign:ShadowAssist.ShadowDepth="Depth1" Style="{StaticResource LogCardStyle}" UniformCornerRadius="8">
                            <Expander IsExpanded="False" Background="Transparent">
                                <Expander.Header>
                                    <Grid Width="550"> <!-- rough width constraint for header content -->
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Orientation="Horizontal" Grid.Column="0">
                                            <materialDesign:PackIcon Kind="History" Foreground="{DynamicResource PrimaryHueMidBrush}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                            <TextBlock Text="{Binding SessionName}" FontWeight="Bold" FontSize="14"/>
                                        </StackPanel>
                                        <TextBlock Grid.Column="1" Text="{Binding StartTime, StringFormat='{}{0:g}'}" Foreground="{DynamicResource MaterialDesignBodyLight}" FontSize="12" VerticalAlignment="Center"/>
                                    </Grid>
                                </Expander.Header>
                                
                                <DataGrid ItemsSource="{Binding Logs}" AutoGenerateColumns="False" IsReadOnly="True" 
                                          HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                                          MaxHeight="300" VerticalScrollBarVisibility="Auto">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="Time" Binding="{Binding Timestamp, StringFormat='{}{0:HH:mm:ss}'}" Width="120"/>
                                        <DataGridTemplateColumn Header="Lvl" Width="50">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Grid>
                                                        <materialDesign:PackIcon Kind="Information" Foreground="#1565C0" Width="16" Height="16" HorizontalAlignment="Center" Visibility="Collapsed" x:Name="InfoIcon"/>
                                                        <materialDesign:PackIcon Kind="Alert" Foreground="#F57F17" Width="16" Height="16" HorizontalAlignment="Center" Visibility="Collapsed" x:Name="WarnIcon"/>
                                                        <materialDesign:PackIcon Kind="CloseCircle" Foreground="#C62828" Width="16" Height="16" HorizontalAlignment="Center" Visibility="Collapsed" x:Name="ErrorIcon"/>
                                                        <materialDesign:PackIcon Kind="CheckCircle" Foreground="#2E7D32" Width="16" Height="16" HorizontalAlignment="Center" Visibility="Collapsed" x:Name="SuccessIcon"/>
                                                    </Grid>
                                                    <DataTemplate.Triggers>
                                                        <DataTrigger Binding="{Binding Level}" Value="Info">
                                                            <Setter TargetName="InfoIcon" Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Level}" Value="Warning">
                                                            <Setter TargetName="WarnIcon" Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Level}" Value="Error">
                                                            <Setter TargetName="ErrorIcon" Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding Level}" Value="Success">
                                                            <Setter TargetName="SuccessIcon" Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </DataTemplate.Triggers>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                        <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="*">
                                            <DataGridTextColumn.ElementStyle>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="TextWrapping" Value="Wrap"/>
                                                </Style>
                                            </DataGridTextColumn.ElementStyle>
                                        </DataGridTextColumn>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </Expander>
                        </materialDesign:Card>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
